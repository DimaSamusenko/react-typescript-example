export const config = {
    api: {
        url: 'http://localhost:3001',
    },
    rootSelector: '.js-app-wrapper',
    authKey: 't1-auth'
};

export const routes = {
    index: '/',
    profile: '/profile',
    login: '/login',
    logout: '/logout',
};
